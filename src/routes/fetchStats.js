// import { PythonShell } from "python-shell";

export default function fetchStats({date, teamAbbr}) {

    // let options = {
    //     args: [date, teamAbbr]
    // }

    // PythonShell.run("../external/app.py", options, (err, results) => {
    //     if(err) throw err
    //     console.log("python says:", results)
    // })

}
